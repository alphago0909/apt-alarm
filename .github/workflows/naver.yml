name: Naver Bot Only

on:
  schedule:
    - cron: '*/10 * * * *'  # 10분마다 실행
  workflow_dispatch:      # 수동 실행

permissions:
  contents: write  # ★★★ 이게 없으면 파일 저장을 못해서 빨간불 뜸!

jobs:
  run-naver:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install requests
        run: pip install requests

      - name: Run Naver Bot
        env:
          # ★★★ 여기를 NAVER_TOKEN으로 바꿨습니다 ★★★
          NAVER_TOKEN: ${{ secrets.NAVER_TOKEN }}
          TG_ID: ${{ secrets.TG_ID }}
        run: python naver_bot.py

      - name: Save Data
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add watchlist.json saved_naver_history.json last_update_id.txt || true
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update Naver Data" && git push)
